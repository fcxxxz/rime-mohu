# Mohu user state

This tracked file keeps `mohu/config/` in release archives. At runtime Mohu
writes `user-ngram.snapshot` here. Release updates must not include or replace
that user-owned snapshot.
