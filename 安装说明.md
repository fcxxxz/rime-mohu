# 魔虎安装说明

## 1. 下载方案包

根据使用的双拼方案下载一个包：

- `rime-mohu-zrm-latest.zip`：自然码
- `rime-mohu-flypy-latest.zip`：小鹤

把压缩包内的全部文件解压到 Rime 用户目录。macOS 通常是：

```text
~/Library/Rime/
```

Windows 小狼毫的用户目录以本机安装位置为准。

两个包分别只启用对应的一个方案，不能把两个包解压到同一个目录后再混用同名文件。

## 2. 放置 V5 模型

从同一个 GitHub Release 下载：

```text
mohu-sentence-ngram-v5.bin
```

放到：

```text
~/Library/Rime/mohu/model/mohu-sentence-ngram-v5.bin
```

以后如果有 `v5.10`、`v6` 等版本，也放在这个目录，文件名保持：

```text
mohu-sentence-ngram-v5.10.bin
mohu-sentence-ngram-v6.bin
```

输入法会按数字版本自动选择最高版本。没有模型文件时会回退普通候选。

## 3. macOS 解除下载隔离

本步骤**仅适用于 macOS**。Windows 用户跳过本节，`.command` 文件也不需要执行。

从浏览器下载的包可能被 macOS 标记为隔离，导致原生引擎弹出“无法验证
`libtigerengine.dylib`”的提示。

解压并复制文件后，在包根目录双击（macOS）：

```text
解除隔离.command
```

也可以在终端执行：

```bash
xattr -dr com.apple.quarantine ~/Library/Rime/mohu
```

这个命令只清理魔虎自己的 `mohu/` 目录，不会清理整个 Rime 用户目录。

## 4. 重新部署

在 Squirrel 或小狼毫菜单中执行“重新部署”，然后在方案列表选择：

- `魔虎·自然码`
- `魔虎·小鹤`

## 5. Rime 同步助手

方案包内包含 `Rime同步助手/`。macOS 双击其中的 `安装.command`，按提示注册
同步任务；同步配置在同目录的 `同步设置.conf` 中修改。

## 常见问题

- **仍然提示无法验证 dylib**：确认已经执行第 3 步，并检查模型/运行时文件位于
  `~/Library/Rime/mohu/` 而不是 `~/Library/Rime/base/`。
- **方案列表没有魔虎**：确认文件直接解压到了 Rime 用户目录，并执行了第 4 步。
- **没有整句候选**：确认模型文件名符合 `mohu-sentence-ngram-vN.bin`，且位于
  `mohu/model/`。
